# Dedicated per-IP limit for password-reset abuse. Rejections are logged at warn
# level and therefore enter the existing nginx/rsyslog monitoring pipeline.
limit_req_zone $binary_remote_addr zone=password_reset:10m rate=5r/m;
